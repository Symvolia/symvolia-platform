/**
 * Symvolia — legacy Canvas 2D intro (retired).
 * The cinematic portal (js/portal/portal-transition.js) now owns the opening.
 * Kept as a no-op so old bookmarks / caches don't throw.
 */
(function () {
  'use strict';
  /* portal-transition.js (ES module) boots the Requiem → Seal metamorphosis */
})();
